{
  "Main": {
    "uid": {
      "type": "UID",
      "config": {
        "label": "Events Calendar"
      }
    },
    "event_calendar_title": {
      "type": "StructuredText",
      "config": {
        "single": "heading1, heading2, heading3, heading4, heading5, heading6",
        "label": "Event Section Title"
      }
    },
    "events_title": {
      "type": "StructuredText",
      "config": {
        "single": "heading1, heading2, heading3, heading4, heading5, heading6",
        "label": "Events Title"
      }
    },
    "event_address": {
      "type": "StructuredText",
      "config": {
        "multi": "paragraph, preformatted, heading1, heading2, heading3, heading4, heading5, heading6, strong, em, hyperlink, image, embed, list-item, o-list-item, rtl",
        "label": "Event Address"
      }
    },
    "event_description": {
      "type": "Text",
      "config": {
        "label": "Event description",
        "placeholder": "This is the message if there is no events description in the calendar"
      }
    },
    "no_events_title": {
      "type": "Text",
      "config": {
        "label": "No Events Title"
      }
    },
    "upcoming_event_title": {
      "type": "StructuredText",
      "config": {
        "single": "heading1, heading2, heading3, heading4, heading5, heading6",
        "label": "upcoming event title",
        "placeholder": "Upcoming Event Title"
      }
    }
  }
}
